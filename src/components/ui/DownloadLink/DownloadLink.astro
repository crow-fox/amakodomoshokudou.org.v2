---
import { getImage } from "astro:assets";
import img from "./assets/icon.svg";

export type Props = {
  href: string;
  filename: string;
};

const props = Astro.props;

const icon = await getImage({ src: img });
---

<a
  href={props.href}
  download={props.filename}
  class="border-primary-border text-md/md inline-grid grid-cols-[1fr_auto] items-center gap-x-8 rounded-xl border px-12 py-8"
>
  <slot />
  <span
    role="img"
    aria-label="をダウンロード"
    style={{
      maskImage: `url(${icon.src})`,
      WebkitMaskImage: `url(${icon.src})`,
    }}
    class="size-[1em] bg-[currentColor] [mask-size:cover] [mask-position:center] [mask-repeat:no-repeat]"
  ></span>
</a>
